<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsc.module.parking.mapper.ComParkingRecordMapper">
    <select id="selectParkingStatistics" resultType="java.util.Map">
        SELECT
        COUNT(*) as totalRecords,
        COALESCE(SUM(fee), 0) as totalFee,
        COUNT(CASE WHEN pay_status = '未缴' THEN 1 END) as unpaidCount
        FROM com_parking_record
    </select>
</mapper>
